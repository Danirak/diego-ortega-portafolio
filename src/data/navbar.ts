import { NavbarConfig } from '../types/config';

export const navbarConfig: NavbarConfig = {
    NAVBAR: [
        { label: "Sobre mí" },
        { label: "Currículum" },
        { label: "Portafolio" }
    ]
};
